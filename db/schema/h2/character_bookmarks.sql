-- ----------------------------
-- Table structure for `CHARACTER_BOOKMARKS`
-- ----------------------------
CREATE TABLE IF NOT EXISTS `CHARACTER_BOOKMARKS` (
  `ID` INT NOT NULL AUTO_INCREMENT,
  `CHAR_ID` INT NOT NULL DEFAULT '0',
  `NAME` VARCHAR(255) NOT NULL DEFAULT '',
  `LOC_X` INT NOT NULL DEFAULT '0',
  `LOC_Y` INT NOT NULL DEFAULT '0',
  `MAP_ID` INT NOT NULL DEFAULT '0'
);
-- ----------------------------
-- Index for `CHARACTER_BOOKMARKS`
-- ----------------------------
CREATE PRIMARY KEY IF NOT EXISTS `CHARACTER_BOOKMARKS_PK` ON `CHARACTER_BOOKMARKS`(`ID`);
CREATE INDEX IF NOT EXISTS `CHARACTER_BOOKMARKS_UK` ON `CHARACTER_BOOKMARKS`(`CHAR_ID`);
