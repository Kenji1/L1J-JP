-- ----------------------------
-- Table structure for `BOARD_AUCTION`
-- ----------------------------
CREATE TABLE IF NOT EXISTS `BOARD_AUCTION` (
  `HOUSE_ID` INT NOT NULL DEFAULT '0',
  `HOUSE_NAME` VARCHAR(255) NOT NULL DEFAULT '',
  `HOUSE_AREA` INT NOT NULL DEFAULT '0',
  `DEADLINE` DATETIME DEFAULT NULL,
  `PRICE` INT NOT NULL DEFAULT '0',
  `LOCATION` VARCHAR(255) NOT NULL DEFAULT '',
  `OLD_OWNER` VARCHAR(255) NOT NULL DEFAULT '',
  `OLD_OWNER_ID` INT NOT NULL DEFAULT '0',
  `BIDDER` VARCHAR(255) NOT NULL DEFAULT '',
  `BIDDER_ID` INT NOT NULL DEFAULT '0'
);
-- ----------------------------
-- Index for `BOARD_AUCTION`
-- ----------------------------
CREATE PRIMARY KEY IF NOT EXISTS `BOARD_AUCTION_PK` ON `BOARD_AUCTION`(`HOUSE_ID`);
